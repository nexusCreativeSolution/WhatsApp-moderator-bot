const { photoEditor } = require('../lib')
const { Module } = require('../lib/commands')
Module(
 {
  name: 'ed1',
  type: 'editor',
  info: 'add view pic Editor.',
 },
 async (client, msg, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed2',
  type: 'editor',
  info: 'jail pic Editor.',
 },
 async (client, msg, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed2',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed3',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed4',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed5',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed6',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed7',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed8',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed9',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed10',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed11',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
Module(
 {
  name: 'ed12',
  type: 'editor',
  info: 'pic Editor.',
 },
 async (client, msg, text, { cmdName }) => {
  await photoEditor(client, msg, cmdName)
 }
)
